# Changelog

## Draft

- Add Cosmos-native PostgreSQL, configuration, runtime, bootstrap, and reverse-proxy graph.
- Make the selected Cosmos hostname the immutable Matrix `server_name`.
- Refuse runtime startup when configuration has not closed or identity has drifted.
- Keep PostgreSQL private and delegate public network attachment to Cosmos.
- Disable browser-oriented SmartShield and bot filtering on Matrix protocol traffic.
